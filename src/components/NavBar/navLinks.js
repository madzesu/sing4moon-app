import { VIEW_ROUTES } from '../../constants';


const navLinks = [
    {
        to: VIEW_ROUTES.SONG_LIST.PATH,
        label: 'Song list'
    },
    {
        to: VIEW_ROUTES.CONTACT.PATH,
        label: 'Contact'
    }
];

export default navLinks;
